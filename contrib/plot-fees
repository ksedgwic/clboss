#!/usr/bin/env python3
import argparse
import os
import re
import sqlite3
from datetime import datetime, timedelta, timezone


def local_tz():
    return datetime.now().astimezone().tzinfo


def normalize_dt(dt):
    if dt.tzinfo is None:
        dt = dt.replace(tzinfo=local_tz())
    return dt.astimezone(timezone.utc)


def parse_ts(ts):
    if ts.endswith("Z"):
        ts = ts[:-1] + "+00:00"
    return normalize_dt(datetime.fromisoformat(ts))


def parse_time_arg(value):
    if value is None:
        return None
    # Ideally, we'd accept the same time arguments as journalctl.
    if value.endswith("Z"):
        value = value[:-1] + "+00:00"
    if re.match(r"^[0-9]{4}-[0-9]{2}$", value):
        return normalize_dt(datetime.fromisoformat(f"{value}-01T00:00:00"))
    if re.match(r"^[0-9]{4}-[0-9]{2}-[0-9]{2}$", value):
        return normalize_dt(datetime.fromisoformat(f"{value}T00:00:00"))
    rel = re.match(r"^([+-]?)(\d+)([smhdw])$", value)
    if rel:
        sign, num_s, unit = rel.groups()
        num = int(num_s)
        delta = {
            "s": timedelta(seconds=num),
            "m": timedelta(minutes=num),
            "h": timedelta(hours=num),
            "d": timedelta(days=num),
            "w": timedelta(weeks=num),
        }[unit]
        if sign == "-":
            delta = -delta
        return normalize_dt(datetime.now() + delta)
    if value.count(":") in (1, 2) and "T" not in value and "-" not in value:
        today = datetime.now().date().isoformat()
        return normalize_dt(datetime.fromisoformat(f"{today}T{value}"))
    return normalize_dt(datetime.fromisoformat(value))


def add_common_args(parser):
    parser.add_argument("peer", help="Peer nodeid (hex).")
    parser.add_argument(
        "--db",
        default=os.path.expanduser("~/bonsai/doc/data/clboss-fees.sqlite3"),
        help="Path to sqlite database.",
    )
    parser.add_argument(
        "--out",
        default=None,
        help="Output image path.",
    )
    parser.add_argument(
        "--show",
        action="store_true",
        help="Show the plot interactively.",
    )
    parser.add_argument(
        "--since",
        dest="from_ts",
        default=None,
        help="Start time (ISO-8601 or HH:MM[:SS]).",
    )
    parser.add_argument(
        "--before",
        dest="to_ts",
        default=None,
        help="End time (ISO-8601 or HH:MM[:SS]).",
    )


def filter_by_time(rows, from_ts, to_ts):
    if from_ts is None and to_ts is None:
        return rows
    filtered = []
    for row in rows:
        ts = parse_ts(row[0])
        if from_ts and ts < from_ts:
            continue
        if to_ts and ts > to_ts:
            continue
        filtered.append(row)
    return filtered


def fetch_rows(db, query, peer):
    conn = sqlite3.connect(db)
    rows = conn.execute(query, (peer,)).fetchall()
    conn.close()
    return rows


def plot_price_level_on(ax_level, ax_mult, rows, peer):
    rows = [r for r in rows if r[1] is not None]
    ts_levels = []
    levels = []
    ts_mult = []
    mults = []
    for t, level, mult in rows:
        ts = parse_ts(t)
        ts_levels.append(ts)
        levels.append(level)
        if mult is not None and mult > 0:
            ts_mult.append(ts)
            mults.append(mult)

    if not ts_levels:
        raise SystemExit("no price_level data for peer in time range")

    level_line, = ax_level.plot(
        ts_levels, levels, linewidth=1, label="price_level"
    )
    mult_line = None
    if mults:
        mult_line, = ax_mult.plot(
            ts_mult, mults, linewidth=1, label="price_mult", color="orange"
        )

    ax_level.set_ylabel("price_level")
    ax_mult.set_ylabel("price_mult")
    if mult_line is not None:
        ax_level.legend([level_line, mult_line], ["price_level", "price_mult"])
    else:
        ax_level.legend([level_line], ["price_level"])


def plot_base_ppm_on(ax_base, ax_ppm, rows, peer):
    rows = [r for r in rows if r[1] is not None and r[2] is not None]
    ts = []
    bases = []
    ppms = []
    for t, base, ppm in rows:
        if base <= 0 or ppm <= 0:
            continue
        ts.append(parse_ts(t))
        bases.append(base)
        ppms.append(ppm)

    if not ts:
        raise SystemExit("no est_base/est_ppm data for peer in time range")

    base_line, = ax_base.plot(ts, bases, linewidth=1, label="base")
    ppm_line, = ax_ppm.plot(ts, ppms, linewidth=1, label="ppm", color="orange")

    ax_base.set_ylabel("base")
    ax_ppm.set_ylabel("ppm")
    ax_base.set_yscale("log")
    ax_ppm.set_yscale("log")

    ax_base.set_ylim(bottom=1)
    ax_ppm.set_ylim(bottom=1)

    ax_base.legend([base_line, ppm_line], ["base", "ppm"])


def plot_size_mult_on(ax, rows, peer):
    rows = [r for r in rows if r[1] is not None]
    ts = []
    mults = []
    for t, mult in rows:
        ts.append(parse_ts(t))
        mults.append(mult)

    if not ts:
        raise SystemExit("no size_mult data for peer in time range")

    ax.plot(ts, mults, linewidth=1)
    ax.set_ylabel("size_mult")

def plot_balance_mult_on(ax, rows, peer):
    rows = [r for r in rows if r[1] is not None]
    ts = []
    mults = []
    for t, mult in rows:
        ts.append(parse_ts(t))
        mults.append(mult)

    if not ts:
        raise SystemExit("no balance_mult data for peer in time range")

    ax.plot(ts, mults, linewidth=1, color="orange")
    ax.set_ylabel("balance_mult")
    ax.set_yscale("log")
    ax.set_yticks(
        [1 / 6, 1 / 3, 1, 3, 6],
        ["1/6", "1/3", "1", "3", "6"],
    )


def plot_price_level(args):
    try:
        import matplotlib.pyplot as plt
        from matplotlib.ticker import ScalarFormatter
    except ImportError:
        raise SystemExit("matplotlib is required to output graphs.")

    rows = fetch_rows(
        args.db,
        "SELECT ts, price_level, price_mult FROM fee_change_events "
        "WHERE peer = ? ORDER BY ts",
        args.peer,
    )
    rows = filter_by_time(rows, args.from_ts, args.to_ts)

    fig, ax_level = plt.subplots(figsize=(12, 4))
    ax_mult = ax_level.twinx()
    plot_price_level_on(ax_level, ax_mult, rows, args.peer)
    ax_level.set_xlabel("time")
    ax_level.set_title(args.peer)

    mult_fmt = ScalarFormatter()
    mult_fmt.set_scientific(False)
    mult_fmt.set_useOffset(False)
    ax_mult.yaxis.set_major_formatter(mult_fmt)

    fig.tight_layout()
    prefix = args.peer[:8]
    out_path = args.out or f"{prefix}-pricelevel.png"
    plt.savefig(out_path, dpi=150)

    if args.show:
        plt.show()


def plot_base_ppm(args):
    try:
        import matplotlib.pyplot as plt
        from matplotlib.ticker import ScalarFormatter
    except ImportError:
        raise SystemExit("matplotlib is required to output graphs.")

    rows = fetch_rows(
        args.db,
        "SELECT ts, est_base, est_ppm FROM fee_change_events "
        "WHERE peer = ? ORDER BY ts",
        args.peer,
    )
    rows = filter_by_time(rows, args.from_ts, args.to_ts)

    fig, ax_base = plt.subplots(figsize=(12, 4))
    ax_ppm = ax_base.twinx()
    plot_base_ppm_on(ax_base, ax_ppm, rows, args.peer)
    ax_base.set_xlabel("time")
    ax_base.set_title(args.peer)

    base_fmt = ScalarFormatter()
    base_fmt.set_scientific(False)
    base_fmt.set_useOffset(False)
    ax_base.yaxis.set_major_formatter(base_fmt)

    ppm_fmt = ScalarFormatter()
    ppm_fmt.set_scientific(False)
    ppm_fmt.set_useOffset(False)
    ax_ppm.yaxis.set_major_formatter(ppm_fmt)

    fig.tight_layout()
    prefix = args.peer[:8]
    out_path = args.out or f"{prefix}-baseppm.png"
    plt.savefig(out_path, dpi=150)

    if args.show:
        plt.show()


def plot_size_mult(args):
    try:
        import matplotlib.pyplot as plt
    except ImportError:
        raise SystemExit("matplotlib is required to output graphs.")

    rows = fetch_rows(
        args.db,
        "SELECT ts, size_mult FROM fee_change_events "
        "WHERE peer = ? ORDER BY ts",
        args.peer,
    )
    rows = filter_by_time(rows, args.from_ts, args.to_ts)

    plt.figure(figsize=(12, 4))
    plot_size_mult_on(plt.gca(), rows, args.peer)
    plt.xlabel("time")
    plt.title(args.peer)
    plt.tight_layout()
    prefix = args.peer[:8]
    out_path = args.out or f"{prefix}-sizemult.png"
    plt.savefig(out_path, dpi=150)

    if args.show:
        plt.show()


def plot_balance_mult(args):
    try:
        import matplotlib.pyplot as plt
    except ImportError:
        raise SystemExit("matplotlib is required to output graphs.")

    rows = fetch_rows(
        args.db,
        "SELECT ts, balance_mult FROM fee_change_events "
        "WHERE peer = ? ORDER BY ts",
        args.peer,
    )
    rows = filter_by_time(rows, args.from_ts, args.to_ts)

    plt.figure(figsize=(12, 4))
    plot_balance_mult_on(plt.gca(), rows, args.peer)
    plt.xlabel("time")
    plt.title(args.peer)
    plt.tight_layout()
    prefix = args.peer[:8]
    out_path = args.out or f"{prefix}-balancemult.png"
    plt.savefig(out_path, dpi=150)

    if args.show:
        plt.show()


def plot_combo(args):
    try:
        import matplotlib.pyplot as plt
        from matplotlib.ticker import ScalarFormatter
    except ImportError:
        raise SystemExit("matplotlib is required to output graphs.")

    size_rows = fetch_rows(
        args.db,
        "SELECT ts, size_mult FROM fee_change_events "
        "WHERE peer = ? ORDER BY ts",
        args.peer,
    )
    balance_rows = fetch_rows(
        args.db,
        "SELECT ts, balance_mult FROM fee_change_events "
        "WHERE peer = ? ORDER BY ts",
        args.peer,
    )
    price_rows = fetch_rows(
        args.db,
        "SELECT ts, price_level, price_mult FROM fee_change_events "
        "WHERE peer = ? ORDER BY ts",
        args.peer,
    )
    base_rows = fetch_rows(
        args.db,
        "SELECT ts, est_base, est_ppm FROM fee_change_events "
        "WHERE peer = ? ORDER BY ts",
        args.peer,
    )

    size_rows = filter_by_time(size_rows, args.from_ts, args.to_ts)
    balance_rows = filter_by_time(balance_rows, args.from_ts, args.to_ts)
    price_rows = filter_by_time(price_rows, args.from_ts, args.to_ts)
    base_rows = filter_by_time(base_rows, args.from_ts, args.to_ts)

    fig, axes = plt.subplots(4, 1, figsize=(12, 12), sharex=True)
    ax_size, ax_balance, ax_price, ax_base = axes

    plot_size_mult_on(ax_size, size_rows, args.peer)
    plot_balance_mult_on(ax_balance, balance_rows, args.peer)

    ax_price_level = ax_price
    ax_price_mult = ax_price_level.twinx()
    plot_price_level_on(ax_price_level, ax_price_mult, price_rows, args.peer)

    ax_base_fee = ax_base
    ax_base_ppm = ax_base_fee.twinx()
    plot_base_ppm_on(ax_base_fee, ax_base_ppm, base_rows, args.peer)

    mult_fmt = ScalarFormatter()
    mult_fmt.set_scientific(False)
    mult_fmt.set_useOffset(False)
    ax_price_mult.yaxis.set_major_formatter(mult_fmt)

    base_fmt = ScalarFormatter()
    base_fmt.set_scientific(False)
    base_fmt.set_useOffset(False)
    ax_base_fee.yaxis.set_major_formatter(base_fmt)

    ppm_fmt = ScalarFormatter()
    ppm_fmt.set_scientific(False)
    ppm_fmt.set_useOffset(False)
    ax_base_ppm.yaxis.set_major_formatter(ppm_fmt)

    ax_base.set_xlabel("time")
    fig.suptitle(args.peer)
    fig.tight_layout(rect=[0, 0, 1, 0.96])

    prefix = args.peer[:8]
    out_path = args.out or f"{prefix}-combo.png"
    plt.savefig(out_path, dpi=150)

    if args.show:
        plt.show()


def main():
    parser = argparse.ArgumentParser(
        description="Plot fee-related time series from clboss fee logs."
    )
    subparsers = parser.add_subparsers(dest="view", required=True)

    price_parser = subparsers.add_parser(
        "price-level", help="Plot price_level over time."
    )
    add_common_args(price_parser)
    price_parser.set_defaults(func=plot_price_level)

    base_ppm_parser = subparsers.add_parser(
        "base-ppm", help="Plot est_base and est_ppm over time."
    )
    add_common_args(base_ppm_parser)
    base_ppm_parser.set_defaults(func=plot_base_ppm)

    size_mult_parser = subparsers.add_parser(
        "size-mult", help="Plot size_mult over time."
    )
    add_common_args(size_mult_parser)
    size_mult_parser.set_defaults(func=plot_size_mult)

    balance_mult_parser = subparsers.add_parser(
        "balance-mult", help="Plot balance_mult over time."
    )
    add_common_args(balance_mult_parser)
    balance_mult_parser.set_defaults(func=plot_balance_mult)

    combo_parser = subparsers.add_parser(
        "combo", help="Stack size, balance, price, and base/ppm views."
    )
    add_common_args(combo_parser)
    combo_parser.set_defaults(func=plot_combo)

    args = parser.parse_args()
    args.from_ts = parse_time_arg(args.from_ts)
    args.to_ts = parse_time_arg(args.to_ts)

    args.func(args)


if __name__ == "__main__":
    main()
